# 生产容器与 Linux 渲染验收

2026-09-08；新源码仓库 `Gradient-Clipping/lazycampus-homepage`。生产构建为 `index-CQX4zw0p.js` / `index-Bx7Kkdc2.css`。

- Docker 使用固定摘要的 Node 24.19.0 构建与非 root Nginx 运行镜像。只读根文件系统、32 MiB `/tmp`、64 MiB 内存与 0.1 CPU 条件下，健康检查、gzip、缓存策略、静态资源 MIME 与缺失资源 404 检查通过。
- 26 项物理与交互状态测试通过。Linux Chromium 153.0.8010.12 的 26 项浏览器检查通过，包含 320 / 390 / 768 / 1024 px、整页与 DPR 2 随机寻道、独立上下文的交互记录回放、用户接管与减少动态效果。
- 等宽代码字体改为随站点提供的 JetBrains Mono 400，增加约 21 KiB WOFF2。`__ready` 在字体明确加载后才完成，消除 Linux 系统等宽字体回退导致的字形与字宽差异。
- 无损导出与浏览器验证关闭 Chromium 部分栅格重绘，消除 3D 边缘受重绘历史影响的舍入差异。断言仍为完整 PNG 的 SHA-256 严格相等，没有放宽像素容差。
- 验证失败时保留整页或交互回放的两份截图及 DOM / Canvas / 时间状态，供后续定位；正常实时浏览继续使用浏览器加速。
- 云端验证与镜像发布记录见仓库的 `Verify and publish homepage` 工作流；公网切换由独立的 `server-gitops` 仓库管理。

---

# v8.1 交付圆点与技术底座布局

2026-09-08；生产预览 4174。构建文件：`index-BQ8bC9vO.js` / `index-sxq6PyPf.css`。

- 交付连接线使用 SVG 原生 CSS 像素坐标，移除非等比 viewBox 拉伸。路径与箭头按实际容器尺寸绘制，圆点保持直径 6 px，与线条共用中线；既有弹簧时间曲线保持不变。
- 连接线几何仅在尺寸变化时重算，窗口缩放时也能在暂停状态更新位置，不增加动画时钟。
- 技术底座改为全宽三层布局：左侧编号与层级标签，右侧应用和平台能力共用三列网格，底部为统一的 Kubernetes 运行环境。连接线表达共享平台能力。
- 手机端将层级标签放在内容上方；图标与标题基线统一，说明按完整词组换行。
- TypeScript / Vite 构建、26 项物理与状态测试、26 项浏览器检查通过。320、390、768、1024 px 无横向溢出，圆点直径、中心线、网格对齐及暂停时窗口缩放均通过；DPR 2 和全页随机寻道、交互记录回放保持像素一致。
- 本轮截图与报告：`renders/v8.1/verification/`，其中 `delivery-dot.png`、`platform.png`、`mobile-platform.png` 为对应改动的实屏记录。下方 v8 记录保留为历史材料。

---

# v8 版式与移动端验收

2026-09-08；Windows、Node.js 24.19.0、Chrome 152.0.7977.82。验收对象为 4174 的生产构建：`index-_KAs4J1H.js` / `index-Ciblt4vy.css`。视觉版式为 v8，确定性时间轴仍采用 v7 的记录格式与接续语义。

## 本轮改动

- 删除移动端电脑下方四个重复快捷按钮，同时移除对应的 DOM 更新与样式。直接点击屏幕内操作；保留重新演示与播放控制。
- 首屏以下重组为五个章节：云原生主视觉、GitOps 交付路径与平台架构、产品案例、品牌理念、联系。
- 使用大字号、较强明暗对比、左右错落的大图与更完整的功能文案；产品仍为 Easy Campus 在前、Smart Shop 在后。
- 莫比乌斯环与 KEEP BUILDING 字样共享页面背景；曲面仍透明，沿环带中线卷动并整体逆时针旋转。
- 联系区采用品牌橙色，大字与小字分别设置对比；原始 SVG 图标保持配色和可辨识细节。
- 修正手机电脑说明与下方分隔线的间距；架构图的箭头样式仅作用于箭头，避免覆盖云图标的位置。
- 无新增动画依赖、时钟或渲染循环。

## 检查结果

- TypeScript 与 Vite 生产构建通过；26 项物理、状态及时间轴测试通过。
- 25 项浏览器检查通过，错误列表为空，详见 `renders/v8/verification/report.json`。
- 320、390、768、1024 px 无横向溢出；每个尺寸均确认屏幕外快捷栏不存在、操作按钮只保留屏幕内四个。
- 390 px 实际点击屏幕提交、扩容与导航；电脑说明未超出场景或覆盖分隔线。减少动态效果时，屏幕提交仍能完成。
- DPR 2 分数秒随机寻道、独立上下文、17 个正反序状态、全页截图与固定手动记录回放保持像素一致。基准 SHA-256：`0d7e5fd3b059b4221e1b4bd6bcd0b63a465e5e5b32c3cd2a5049f9d39054334a`。
- 原笔记本透视、逐字高亮代码、用户接管后的指针接续、平滑锚点、产品素材、图标及联系链接均通过回归检查。

## 本机性能采样

加速 Chrome；每种尺寸采集 180 帧；未与截图或构建并行运行。完整原始数据在 `renders/v8/performance.json`。

| 视口 / DPR | 平均 seek | 帧间隔 P95 | 大于 25 ms 的帧 |
| --- | --- | --- | --- |
| 1440 × 960 / DPR 1 | 5.65 ms | 16.70 ms | 1 / 180 |
| 1440 × 960 / DPR 2 | 15.29 ms | 16.80 ms | 3 / 180 |
| 390 × 844 / DPR 2 | 6.13 ms | 16.70 ms | 0 / 180 |

## 视觉记录

- `renders/v8/verification/desktop-full.png`、`mobile-full.png`：桌面与手机全页。
- `capabilities.png`、`approach.png`、`products.png`、`about.png`、`contact.png`：新版各章节。
- `mobile-hero.png`、`mobile-capabilities.png`、`mobile-about.png`：手机实屏细节。
- 确定性逐帧导出与 FFV1 无损压制脚本保留，使用方法见 README。下方 v7 的无损样片和截图是历史验收材料，像素不代表新版布局。

---

# 历史：v7 验收记录

2026-09-08；Windows、Node.js 24.19.0、Chrome 152.0.7977.82。针对 4174 端口上的生产构建。

## 本轮结果

- 删除屏幕右上角“交互演示”，电脑内只展示云原生开发。
- 演示从无侧栏的 VS Code 简化界面开始：逐字输入高亮 TypeScript 代码，提交后构建并过渡到服务运行状态。
- 部署更新、3 / 5 副本扩缩容、故障自愈、回滚和实例查看均改变模型状态。
- 用户点击后，引导指针直接观察结果、接续下一个有效步骤；不重新点击刚完成的按钮。再次介入会替换未来接续计划，保留当前指针位置、速度和正在进行的操作。
- 删除固定左上角停靠点。观察位置跟随目标实例与操作类型，曲线按当前方向弯曲。
- 莫比乌斯环直接融入页面；环带截面绕自身中线卷动，同时整体缓慢逆时针旋转。中心线保持不动，不再绕世界 Y 轴向内外翻转。
- 窄边框大屏幕、向外倾角、低视角键盘、原始图标、产品概念图和联系邮箱保持正常。

## 功能与确定性

- TypeScript 检查和 Vite 生产构建通过。
- **26 项数值与状态测试通过**：代码前缀、语法色彩、解析弹簧、C1 指针、240° 凹角、用户操作后接续、记录回放、环带固定中线与 Möbius 拓扑、选帧，以及保留的软环和绳索原语。
- **24 项浏览器检查通过**：`renders/v7/verification/report.json`。
- 17 个代码输入、提交、扩缩容、自愈、循环边界与 200 秒时间点，正序 / 逆序 PNG 相同。
- DPR 2 分数秒随机寻道、延迟截屏和独立浏览器上下文像素相同，基准 SHA-256 为 `32a102ebd574c77d8a5657691ea23c87d9575d6f77a33f4a11a3b3c69ab32756`。
- 固定手动记录在独立上下文的 6 个时间点逐像素一致，包含提前提交、输入补齐、编辑器过渡、实例查看与后续命令接续。
- 浏览器实测：手动提交后指针跳过提交按钮，接续到扩容，版本保持 v2，没有重复部署。
- 查看实例不会重启原发布或修复；冲突操作禁用；没有上一版本时回滚禁用；恢复演示与下载记录正常。
- 环带透明角像素 alpha 为 0，容器背景透明；任意寻道可复现相同曲面，288 秒回到同一角度与像素。
- 全页截图包含活动曲面，在不同寻道历史后相同。
- 每隔 6 秒检查完整 288 秒环带过程，未发现边缘裁切。
- 默认播放、暂停、外部寻道停止全部动效、负值归零和非有限值拒绝通过。
- 390、768、1024 px 布局无横向溢出；390 px 验证快捷提交、随后手动扩容和移动导航。
- 减少动态效果时不自动播放，环带静止；明确提交立即呈现完成状态。
- 内部导航经过中间滚动位置再落到目标；产品顺序、链接、素材和图标引用正确。
- 无页面异常、控制台错误或 HTTP 错误。

固定浏览器、字体、素材、DPR、布局、输入记录、焦点与滚动位置属于像素验证边界。不同用户记录允许产生不同过程。

## 本机性能

正常浏览器加速，每组 120 次同步寻道、180 个 rAF 间隔、12 次完整意图处理。手机尺寸为本机视口模拟，不是实体手机。

| 视口 / DPR     | 平均寻道 | 平均帧间隔 | 95 分位帧间隔 | 超过 25 ms | 平均操作处理 |
| -------------- | -------: | ---------: | ------------: | ---------: | -----------: |
| 1440 × 960 / 1 |  3.80 ms |    8.75 ms |      16.60 ms |    0 / 180 |     19.60 ms |
| 1440 × 960 / 2 |  7.17 ms |   11.20 ms |      16.80 ms |    0 / 180 |     20.61 ms |
| 390 × 844 / 2  |  3.92 ms |    9.21 ms |      16.60 ms |    1 / 180 |     17.12 ms |

原始数据为 `renders/v7/performance.json`。当前机器为高刷新率环境；结果不是跨设备帧率保证。默认物理缓存为 614,480 字节，用户操作预计算剩余步骤响应。曲面使用扫描线、深度缓冲和每帧一次像素上传；没有逐帧物理积分或 React 组件重渲染。

## 视觉证据

- `renders/v7/verification/hero.png`：最终 VS Code 首屏与笔记本几何。
- `renders/v7/verification/code-typing.png`：逐字输入。
- `renders/v7/verification/deployment.png`、`scale.png`、`self-healing.png`、`rollback.png`：云原生过程。
- `renders/v7/verification/about.png`：融入页面的环带。
- `renders/v7/verification/desktop-full.png`、`mobile-full.png`：完整响应式页面。
- `renders/v7/mobile-code.png`：移动端代码界面；`renders/v7/about-0.png`、`about-12.png`、`about-24.png`、`about-36.png`：多个卷动角度。最终文字和阴影以 verification 截图为准。
- 两张产品概念图及代码依据 / image_gen 提示词仍在 `public/media/`。

修改仅在独立展示项目中进行，没有改变生产环境事实或发布链路。旧版 renders 目录保留历史产物。

## 无损渲染验证

`renders/v7/lossless-dpr2/master-ffv1.mkv`

- 时间轴版本 7，加载 `renders/v7/verification/interaction-recording.json`，包含 7 次真实浏览器操作。
- 1440 × 960 CSS px、DPR 2，输出 2880 × 1920。
- 区间 `[2.5, 5.5)` 秒，60 fps、3 秒、180 帧，覆盖用户提前提交、代码补齐、进入运行界面与中途查看实例。
- FFV1 level 3 / BGR0，**180 / 180 帧解码 RGB24 framemd5 与源 PNG 相同**。
- 原始 PNG、精确时间、SHA-256、渲染环境和完整交互记录保存在 `frames/` 与 `manifest.json`。

```sh
npm run render -- --url http://127.0.0.1:4174 --recording renders/v7/verification/interaction-recording.json --start 2.5 --duration 3 --fps 60 --dpr 2 --out renders/v7/lossless-dpr2
```

重跑请使用新的输出目录。完整默认电脑演示省略 `--recording`，使用 `--start 0 --duration 32`；环带完整共同周期使用 `--section about --duration 288`。

## VS Code 图标更新

用户提供的 `Visual Studio Code 图标_1788840845.svg` 已从下载目录重命名并移动至 `public/icons/visual-studio-code.svg`，替换编辑器标题栏原有的简化绘制图标。源文件与移动后的 SHA-256 相同：`ecee4b2b762e0ae6e0583eb1445e47bb2e5919837612ce69a174d02e86097b48`。

本次构建通过；浏览器确认加载项目内原始 SVG，只有一次图标请求，随机寻道后的页面像素一致且无控制台错误。图标保持原始配色、渐变和阴影，并复用初始化 Canvas 缓存。记录与截图在 `renders/v7/icon-update/`。此前的 v7 全套截图和无损样片保留为图标替换前的验收记录。
