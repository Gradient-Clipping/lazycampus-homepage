# 确定性物理时间轴 · v7

首页使用 React、CSS 3D、Canvas 2D 和 SVG。窄边框笔记本保留轻微向外倾斜的大屏幕与低视角键盘；内容从简化的 VS Code 高亮编辑器开始，提交后进入同一个云原生服务的部署、扩缩容、自愈与回滚过程。莫比乌斯环直接融入页面，绕环带中线卷动，并整体缓慢逆时针旋转。

## 唯一时间入口

`window.__seek(t)` 同步更新 DOM、Canvas、SVG 和 HUD，并停止实时预览。普通访问先同步绘制电脑并播放，字体和图标缓存随后补齐，产品画面接近视口时才加载；这些下载失败不会阻止电脑交互。`__ready` 在普通访问中等待首屏增强资源结束，在 `?render` 导出模式中严格等待字体、全部产品素材、原始图标和键盘缓存准备完毕。异步资源使用 `repaintTimeline()` 重绘当前时刻，不暂停播放、不重置用户操作。预览适配器只计算 `start + (timestamp - epoch) / 1000`，再进入同一个 `__seek`；没有 `t += dt`、CSS keyframes、异步补间或逐帧物理积分。

确定性定义为 `render(t; fixedRecording, fixedAssets, fixedViewport)`。用户点击改变不可变意图记录；对固定记录，任意时间和任意寻道顺序得到同一画面。不同输入记录自然可以产生不同结果。

```text
绝对时间 t + 不可变用户意图记录
 ├─ 单一 C1 Hermite 曲线 → 指针位置与解析速度
 ├─ 240 Hz Float32 查表 → 笔记本、四个磁吸按钮
 ├─ 代码前缀 → 语法高亮、逐字输入、提交状态
 ├─ 云原生模型 → 版本、副本、滚动更新、恢复与回滚
 ├─ 环带内禀卷动 + 整体逆时针旋转 → 曲面、法线与深度
 └─ 素材帧索引 → 预解码产品图像
                  ↓
同屏坐标 + 统一 CSS 3D 投影 → DOM / SVG / Canvas → PNG
```

电脑演示为 32 秒，环带卷动为 72 秒，整体逆时针旋转为 96 秒。初次入场完成后，默认页面共同周期为 288 秒。时间不限于这些周期；例如 `__seek(200)` 对应电脑相位 8 秒。旧软环和绳索原语保留自己的 24 秒测试场景，不参与首页时钟。

## 从代码到运行

`code.ts` 保存一个简短的 TypeScript HTTP 服务示例与语法 token。代码按字符前缀出现，行间有阅读停顿；关键字、标识符、字符串、函数和数字分别高亮。编辑器没有侧栏、缩略图或密集工具条，提交按钮与运行界面使用相同物理坐标。

默认过程：

|     时间 | 内容                                           |
| -------: | ---------------------------------------------- |
|   0–6 秒 | 在简化 VS Code 中逐字编辑服务                  |
|   6.4 秒 | 提交并部署；完成输入、构建镜像，过渡到运行界面 |
|    12 秒 | 扩容到 5 个副本                                |
|    16 秒 | 模拟失联，自动恢复副本                         |
|    20 秒 | 缩容到 3 个副本                                |
|    24 秒 | 回滚到上一版本                                 |
| 30–32 秒 | 回到编辑器，准备下一轮                         |

提前点击提交会快速补齐剩余代码，然后进入构建与运行过程。过渡由解析临界阻尼控制，代码光标的闪烁同样由绝对时间求值。

## 用户操作后的接续

`Experience.record(action, time, target?)` 保存 `{time, action, target?}`。`action` 为 `deploy`、`scale`、`fault`、`rollback` 或 `inspect`；实例索引为 0–4。

新分支保留当前引导指针的位置和解析速度、笔记本与按钮的弹簧位置和速度。用户已经完成点击，因此接续曲线直接指向操作结果，跳过已点击的按钮。观察位置随部署、扩容方向、故障实例和查看目标变化，没有固定的左上角停靠点。

之后根据当前状态编译剩余步骤：提交后继续扩容、自愈、缩容与回滚；扩缩容后继续故障恢复及可用的回滚；故障后只在存在上一版本时接续回滚。查看实例保留尚未执行的后续步骤，同时保留正在进行的操作的原始开始时间。后续自动操作只在前一步完成并留出观察时间后执行，激活时指针停在对应按钮上。

再次介入会替换该时间点之后的接续计划，已经完成的操作不会重做。每个分支根据剩余步骤长度预计算物理响应，最后留出 5 秒收敛；完成后指针停留，结果保持。环带继续使用同一个预览时钟运动。只有“重新演示”会清空用户记录。

`window.__timeline.recording` 提供只读记录，HUD 可下载 JSON。初始化前设置 `window.__initialRecording` 可重放；渲染脚本通过 `--recording file.json` 注入。记录必须按时间排序、时间有限且非负，操作与目标必须有效。冲突命令不会写入记录。v7 使用新的接续语义；旧 `{time, view}` 格式不支持，旧版记录不保证得到相同过程。

## 云原生状态模型

`cloud.ts` 包含纯函数模型投影。状态包括期望副本、当前版本、上一版本、所选实例、编辑器状态和当前操作。每次操作保存开始时间、持续时间与此前版本/副本；渲染根据这些输入直接求值。

| 操作        | 可见结果                                 |     操作时长 |
| ----------- | ---------------------------------------- | -----------: |
| 提交 / 部署 | 构建镜像后逐个更新实例版本，保存上一版本 |       3.8 秒 |
| 扩缩容      | 3 / 5 副本切换，新实例经历启动与就绪     |       2.2 秒 |
| 模拟故障    | 所选实例失联、淡出、重建，就绪数先减后增 |       2.8 秒 |
| 回滚        | 实例逐个恢复为上一版本                   |       2.6 秒 |
| 查看实例    | 选择高亮，显示版本和状态，再次点击取消   | 不打断原操作 |

未选实例时故障作用于首个可用实例。运行操作尚未完成时禁用冲突命令，仍允许查看实例；没有上一版本时回滚不可用。编辑器阶段只显示提交入口。所有操作都发生在本地概念模型中，不调用生产集群。

## 解析弹簧与形变

方程为 `x'' + 2ζωx' + ω²(x-target) = 0`。欠阻尼分支令 `a=ζω`、`wd=ω√(1−ζ²)`、`y=x0−target`：

```text
x(dt) = target + exp(−a·dt)[y cos(wd·dt) + (v0+a·y)/wd sin(wd·dt)]
v(dt) = exp(−a·dt)[v0 cos(wd·dt) − (a·v0+ω²·y)/wd sin(wd·dt)]
```

临界与过阻尼有各自解析分支。每个 1/240 秒区间内保持外力目标常数；状态转移矩阵预先编译，缓存构建只做乘加。

| 组件            | ω rad/s |    ζ | 响应                                           |
| --------------- | ------: | ---: | ---------------------------------------------- |
| 笔记本          |      12 | 0.92 | 光标位置与速度的小幅反馈                       |
| 四个磁吸按钮    |      23 | 0.72 | 104 逻辑像素作用半径，距离平方衰减叠加光标速度 |
| 实例横向重排    |      11 | 0.88 | 新旧副本布局间的解析弹簧                       |
| 实例创建 / 自愈 | 11 / 12 |  0.9 | 轻微竖向位移与恢复                             |
| 实例更新        |      14 |    1 | 12 逻辑像素抬升与回落                          |
| 初次入场        |     9.5 |    1 | 28 CSS px 位移                                 |

切向拉伸、法向倒数压缩，静止时恢复；笔记本最大形变 1.2%，按钮 3.5%，实例 2.5%。方向模糊与速度方向一致，静止归零。默认物理缓存为 614,480 字节；每条手动记录新增 `(duration × 240 + 1) × 20 × 4` 字节，时长由剩余步骤决定。

## 指针、点击区与硬件

指针骨架为 `(0,0)`、`(16+16cos15°,16−16sin15°)`、`(16,16)` 及第二点的镜像。关于 `y=x` 对称，内部凹角为 240°；二次曲线圆角保留对称性。指针始终朝向左上，运动方向由曲线决定，两者没有混用。

按钮、实例和指针共享 1000 × 564 逻辑坐标。Canvas 元素和透明 DOM 点击区使用相同位移、旋转与形变，按 0.668 比例转换到 CSS 像素再投影。DOM 提供键盘操作、可访问名称、禁用状态、选择状态与实时播报。

笔记本外壳 680 × 394 CSS px，屏幕 668 × 376.752，左右边框约 5 px。整机 `perspective(2400px) rotateX(-7deg) rotateY(-7deg)` 并轻微平面旋转，顶部朝观察者倾斜。键盘原始 SVG 为矩形，各排按键、扬声孔和触控板共处一平面，仅整体 `rotateX(85deg)` 投影；前沿自然比铰链沿更宽。

SVG 键盘在 `__ready` 前转为固定 1248 × 620 内存位图，避免 Chromium 的小字号刻字受历史 3D 变换影响。屏幕壳使用独立投影平面，保证静止按钮也能正确命中。

## 环带的内禀运动与 Canvas 光照

在环带自身坐标中，中心线为 `C(u)=(146 cos u,146 sin u,0)`，横向位置 `v∈[-1,1]`，半宽 68：

```text
a = u/2 + flow(t)
P(u,v,t) = ((146 + 68v cos a) cos u,
            (146 + 68v cos a) sin u,
             68v sin a)
```

`v=0` 的中心线在卷动中保持不动；每个截面围绕其切线旋转。半扭转关系 `P(0,v,t)=P(2π,−v,t)` 始终成立。随后应用固定的观察倾角和绕画面中心的负角度旋转，产生整体逆时针运动。没有绕世界 Y 轴的内外翻转。

网格为 192 × 12，2,509 个共享顶点、4,608 个三角形，拓扑和数组只分配一次。每帧直接计算曲面和解析法线，插值双面光照；扫描线只遍历三角形覆盖区，深度缓冲处理自遮挡，最后一次 `putImageData`。这避免大量 Canvas 渐变路径的绘制开销，并消除平面着色接缝。像素、透明背景投影和深度缓冲仅在尺寸改变时重新分配。

## 素材与验证边界

Canvas 按实际显示尺寸 × DPR 栅格化。静态屏幕底图、产品图、SVG 图标和键盘使用缓存；React 不逐帧重新渲染。原始 Kubernetes、Git、Flux 图标保持内部颜色和细节，深色区域使用浅色底座。Easy Campus 概念图位于 Smart Shop 上方，两者保留 image_gen 素材和提示词；真实录屏帧接口默认关闭。

原有软环和 Verlet 绳索仅作为测试覆盖的可选原语保留：软环为 72 个径向质点和离散 Laplacian，绳索为 22 节点、240 Hz Verlet 与每步 12 次距离约束。

固定浏览器、系统字体、素材、交互记录、视口、DPR、焦点和滚动位置后比较像素。浏览模式保留平滑锚点，`?render` 直接定位。减少动态效果时环带静止，不自动播放；明确点击立即寻道到该操作完成的状态，并停在后续自动步骤之前。

验收覆盖代码输入、命令接续、逆序寻道、独立上下文回放与移动端操作。无损导出固定 sRGB、软件光栅化和帧时间；FFV1 解码后的逐帧 RGB24 framemd5 必须与源 PNG 一致。不承诺跨操作系统、字体和浏览器版本的像素天然一致。
